
# 最優先対応

## コードベースの肥大化をどうにかする

- 実装・テストのコードベースが肥大化した結果、トークン消費がエグいことになっている
- エグすぎてまともにワークロードを実施出来ないような状態なので、どうにかする必要がある
- TODO
    - テストの立ち位置について追記
    - 実装・テストの最小化原則を oracles に追記
    - ファイル分割・ディレクトリ階層化原則を oracles に追記
    - apply 関係のプロンプト生成を oracles python 関数化
        - AI に書かせると `realization standards に従い、realization files の肥大化抑制も確認してください。` みたいなアホプロンプトを生成しやがるので、絶対に人間握らなければいけない
        - Structured output schema の書き方は dataclass とレンダラーの組み合わせでどうにかしたい
    - 既存の実装・テストを全削除、 apply を実行

## oracles ファイル編集のワークフローが回るようにする

- ワークロードを実施する上で oracles 編集がボトルネックなので、優先して解決したい
- 普通のプログラミングで言う所の「コード書いてビルドエラー読んで…」みたいなイテレーションを高速で回せるようにする
- TODO
    - review oralces 上の `codex exec` 呼び出しのプロンプトを oracles ソースコード化
    - apply 実行して仕様変更を反映
    - review oracles の裁判モデルに high を使うのは重すぎる
        - １周で良いでしょとか、 FATAL だけで良くない？　とかある
    - プロンプトが満たすべき基本要件の定義は関数化したい
    - 実際に `cmoc review oracles` を使って oracles の改善を図る

# cmoc の作業品質

## standards.md の内容に問題が無いかチェック

- ChatGPT Pro に吐かせただけで、中身の妥当性は精査していない
- 例えば「oracles files はテキスト化された人間意図である」とか、AI には判定しようがなくないか

## review, apply の所見・要修正点の取り回しを共通化

- かなり類似しているので、お互いに良いところを真似したり、共通化出来るところは共通化したい
- e.g. 所見のマージとか
- 差が出るのがしょうがない部分もあるので一定程度は妥協する

## worktree 周りが終わってる

- `<cmoc-apply-worktree>` 側にログファイルが出力されてしまう
- 非常に読みにくいのでどうにかしたい
- ていうか `<cmoc-apply-worktree>/.cmoc` 読みに行ってたら、ブランチステートとかを正しく読めないのでは？
- oracles に追記が必要
- `<cmoc-root>`, `<repo-root>` 系の呼び分けは再検討が必要
    - 以下の 4 択の呼び分け
        - cmoc のルート (`<cmoc-root>`)
        - cmoc の作業対象リポジトリのルート (`<repo-root>`)
        - 作業隔離用ワークツリールート
        - `<repo-root>` か作業隔離用ワークツリールートのどちらでも OK

## 各プロンプトをきっちり仕様化

- 今は指定が雑なので AI エージェントの仕事も雑
- AI エージェントがサボれないよう、きっちり組む必要がある
- 多分 oracles 上に python 関数として書いたほうが良い

## テスト実行パス

- 汚しても良いところでトイプログラムで実行ということにする
- パスとかトイプログラムの仕様とかを決めないといけない

## 無茶な仕様を調べる方法が欲しい

- 今は `oracles` にかかれている仕様が絶対視されている
- `oracles` の仕様の品質は AI レビュー＋人間修正のループによって保たれる
- 問題は、このレビューの際に「実装は見てはいけない」としている点
- 「言われた通りに頑張って実装したけど、やっぱこの仕様おかしいッスよ」を拾えるフローがどこにもない
- `review oracles` の機能として組み込みたい

## 「仕様に書いてないことが実装されている」をどう扱うか

- 例えば、単純に oracles ファイルを削除したりすれば「実装は存在するが、そのことは仕様には書いてない」は簡単に起きうる

## Structured Output の schema で OpenAI の API 制限を組み込む

- required は全部に付けないとダメらしい
- ていうか fork の schema にハッシュ含めるのが違うって話かも？

## 要修正点リスト改善が停止するわけがない

- 入力した要修正点リストがそのまま返ってきたら～なんて条件満たせるわけがない
- 多分、リストをそのまま返すことにしてるのが良くない。

## 要修正点にも defender を入れたほうがいいかも

- その修正、本当に必要ですか？　過去の修正と矛盾してませんか？　を挟む
- それにより、余計なおせっかいを削り取る

## ネーミングの支援も必要

- たとえば「oracles ファイル記述標準」とか
- AI はネーミングから実態を類推しがちなので、正しいネーミングはとても重要

## oracles と実装っていう呼び分けが適切じゃないかも

- oracles 配下に人間責任のソースコードを置くこともあるから、それと実装とで呼び分けるのはちょっとダルい

## `cmoc apply fork` の rolling, session モードで、変更があった箇所を補足情報として渡す

- ファイル別に問題点を列挙するフェーズの話
- 「変更があったのここですよ」は事前に機械的に生成して、LLM に渡す
- ねらい
    - LLM が初期調査でトークンを消費するのを抑制する
    - LLM の調査漏れを防ぐ
- 削除に対する追従も必要な事を考えると git diff の結果を流し込むべきか
    - oracles スナップショット参照原則と真正面から衝突するので、ここは原則の方を組み直す必要がある

## cmoc が生成するプロンプトと、リポジトリ固有指示との整合性チェックが欲しい

- 「リポジトリ側の `AGENTS.md` なり oracles ファイルなりで書いた指示」と「cmoc のプロンプト由来の指示」とが矛盾する可能性は当然ある
- この矛盾が発生しているかどうかは cmoc 側でチェックしてほしい
- `cmoc review oracles` とかだろうか？
- しかし `AGENTS.md` は oracles ファイルに含まれないので、別のサブコマンドが必要か？

## テストコードについての言及が弱い

- oracles ファイルの内容に基づいてテストコードが生成されるということを、ちゃんと言及していない箇所がまぁまぁあるかも
- あと、実装とテストの関係性が宙ぶらりん
    - コードベースの肥大化問題から考えれば、テストは AI 作業用のスクラッチバッファみたいな扱いにするしかなさそう
    - AI が円滑に作業をするためにテストを書くことは許容するが、いわゆる「テスト」として大事にすることはない
    - 回帰テストが必要なら、それは oracles 側に引き上げるべきか

## 用語は英語での定義が必要

- oracles ファイル上は日本語だけが決まっていれば良い
- 実装する上では英語の命名も仕様で定めておかないと、実装上命名がブレる可能性がある
- 普通に良くないので日英両方定義しておくとか、用語は英語に限定するとか、何かしらの制限が必要
- 検索性を考えたら、英語限定にするしかなさそう

## 所見リストのマージと改善は別枠にしたほうが良い

- 改善作業には高価な高性能モデルが必要だが、高性能モデルは本当に高い
- 先に中性能モデルで所見リストをコンパクションして高性能モデルに渡す、高性能モデルによる改善は１～２周だけ、みたいな削減施策が必要

# 実行時間とトークン消費

## 並列処理もうちょっと賢くする

- global quota gate を入れる
    - 1 つのスレッドが代表して poll をする
    - N スレッドが一斉に poll みたいなのはやらない
- poll は model class ごとにやる
    - gpt-5.5, gpt-5.4-mini は別々の poll にする
    - 全部通ったら再開にする
- quota poll の消費トークン数を削減
    - 多分 AGENTS.md とかを読みに行っていて、無駄に消費している
- review oracles の並列数上限
    - 5h limit は 2~4 並列くらいでも十分到達出来る

## サブコマンド実行の所要時間を書き分けたい

- 並列処理するようになったので、エージェント実行時間の総和（もしも直列実行してたらの総時間）も欲しい

## 作業の停止性を良くしたい

- 反復オーケストレーションは停止性っぽい問題を抱えている
- e.g. Codex CLI が消化した以上に Codex CLI 自身が仕事を作る --> 反復のハードリミットいっぱいまで作業
- e.g. イテレーションを跨いで編集合戦を行う --> 循環参照的な構造が発生、反復のハードリミットいっぱいまで作業
- つまり、ハードリミットに達した時に、本当に作業が残っているのか、無駄な作業を無駄に作りまくっているのか、この区別が付かない
- この不安を払拭するために、工夫を入れたい
- 問題たりえるのは「Codex CLI が生成する仕事量」だけなので、ここを減らすしか無い

## 「良かれと思って」を抑制したい

- oracles・実装が膨らむ事＝トークン消費の増加なので、出来るだけ小さく保ちたい
- できれば、 `cmoc apply fork` の段階から、コード膨らみにくいように誘導したい
- そのためには「良かれと思って」系をしないように抑制するような工夫が必要
- 単にプロンプトで指示するだけはなく、Structured Output でわざと「良かれと思って」を吐き出させて、それを無視するのが良いだろう

## デカい oracles ファイルを分割したい

- デカい oracles ファイルは調査対象が多いので、それに対する Codex CLI 呼び出しの負担が増えて、調査結果の抜け・漏れが懸念される
- 大きすぎるファイルの分割は `cmoc review oracles` で面倒を見たい
- しかし、大きすぎるってどのくらい？
    - schema 定義は独立したファイルにしておこう、くらいは思いつく
    - 大きさじゃなくて、意味論を基準にしたほうが良いかも
- スキーマ定義は独立したファイルにしたくなる
    - Structured Output schema みたいに、 description として書き込むのが良さそう？
    - いや、そもそも json schema の専用の記法とか無いの？

## `INDEX.md`

- `INDEX.md` のメンテナンスにかかるトークン消費が意外とエグそう
    - chached input が効いているので、実は大したコストではない説はある
- 修正量の多さと意味的な重要さは必ずしも比例しない
    - e.g. typo が忠実に実装されてしまっていたので修正した --> たとえ１文字でも確実に `INDEX.md` に
- 量的に僅かな修正であっても意味的な重要な修正である可能性を考えると、全体を再生成せざるを得ないというのが厳しい
- 非エージェントの検索システムが必要なのでは
- そこまでやり始めたらもはや minimal ではないので、派生した別プロダクトかも
- `INDEX.md` を完全に並列にしたい
    - 以下のルールでいけそう？
        - `INDEX.md` を生成するのに `INDEX.md` を読んではいけない
        - ハッシュ値の計算に `INDEX.md` を含めてはいけない
- ファイル・ディレクトリで性質が違うかも
    - そのファイル単体を見ただけでは、意味のあるルーティング情報は作れないのでは？
    - リポジトリの全体像を理解していないと、正しいルーティング情報は作れないのでは？
- ルーティングでは根本的に不十分
    - 「ルーティングに従って関係のある情報を見つけたからこれでOK」みたいな感じで調査をサボる可能性がある
    - 

## `review oracles` に用語統一機能を組み込みたい

- 用語リストに基づいたファイル別チェックを駆使すれば、かなり安く行けるはず。
- 修正が漏れても良いタイプなので、 reasoning effort は低くても大丈夫なはず
- oracles の規模が大きくなってきて、用語統一が大変だと感じるようになってきたので、早めに欲しい
- あと、 AI に提供するインデクシング情報としても有用なはず
    - ワードの統一が保証されているのであれば、検索ベースの関連文章検索の信頼度が上がる

## `review oracles` で読むべきファイルのリストをより正確にリストアップしたい

- `INDEX.md` とは別の、依存関係グラフが必要
- 依存関係情報を `INDEX.md` に書き込むのもアリかもしれない（see also として役に立つはず）
- 「意味的に関係があるか？」の判断のコアは Codex CLI になるが、機械的に処理可能な部分の責任は cmoc が持ち、 Codex CLI が担う責任範囲は最小化する

## 読むべきドキュメントの候補を機械的に列挙してプロンプトに注入する

- 今は、何を読むかは AI 任せになっているが、この判定処理にもトークンが消費される
- ワード検索などで機械的に列挙してファイルリストをプロンプトに注入することで、トークン消費の緩和を狙う
- 過去の Codex CLI 呼び出し履歴から、実際に行われているテクニックを抽出する
- 多分、あえてコマンドの実行結果（検索コマンドとか）をそのまま流し込んだ方が AI には伝わる

## トークン消費の傾向を分析出来るようにしたい

- 分析に必要な情報を全部ログに出すようにする
- やりたいこと
    - そのサブコマンドの実行にかかった費用 (USD ベース) を試算
    - どの処理にどのくらい費用を食われているかをプロファイリング

## キャッシュヒット率を上げたい

- cached input のほうが圧倒的に安い
- キャッシュが効くかどうかは exact prefix match らしい
- 既にかなりキャッシュが効いているので、まだ頑張れる余地はあるかも

## 各種ファイルの分割・構造化は思ってたよりも重要そう

- バカデカいファイルは罪深い
    - Codex がファイル全体を読んじゃった時のトークン消費が重たい
    - ルーティング情報が意味をなさない可能性がある
- apply で継続的に取り組む必要がある
    - 一度悪い状態になったリポジトリをキレイにするのには多大なトークン入力を必要とするから「トークン消費を抑えるためのリポジトリのメンテナンス」は apply に組み込んで継続的に実行したほうが良い
    - リポジトリの総行数の削減は目標として良さそう
    - ファイル分割については、過剰な分割にならないように工夫が必要

## misc_specs.md を抹消したい

- 普通に考えて、ちゃんとカテゴライズするべき

# cmoc ワークフロー

## 通知

- WSL から Windows のトースター通知を叩けるらしいので、仕様を追加したい

## Codex CLI が oracles を触っちゃったら話

- `ownership_and_safety.md` で書いてる内容、他の箇所でも言及していた気がする
- サンドボックスで強固にガードしたい

## cmoc branch が未コミット差分ありでも通る仕様に戻したい

- 普通に不便なので、未コミット差分は許容したい

## Codex CLI 更新チェック

- 最大で１日１回の自動更新処理は cmoc に組み込んでおきたい

## バグ修正は正式なワークフローとして組み込んでも良いかも

- バグ修正は状態遷移的な操作なので、既存の oracles - apply ループには乗せにくい
- `oracles/bugs` とかに過去のやらかし事例を集積し、それらと同じ轍を踏んでいない事を apply の過程でチェック、とかだろうか

## 使用モデル設定をプロファイル由来にしたい

- 今はモデルと Reasoning Effort を引数で直接指定している
- これだと、モデルセットが更新された時に cmoc 側の追従が必要でかなりダルい
- なので `<repo-root>` 側の設定として cmoc 用のプロファイルを書いてもらうことにする
- `cmoc dector` サブコマンドが必要だろう

## README.md も AI によるメンテの対象にしたい

- README.md の仕様を oracles に書けば、適切な README を書いてくれるはず

## `cmoc session fork` で過去のセッションの状態を検証させてはいけない

- 古いステートファイルが検証で引っかかって…みたいな事が平気で起きる

## `review oracles` もスナップショット固定が必要

- apply と同様にワークツリーを作って、そこで `INDEX.md` の修正をコミット
- その review 用ブランチの HEAD で worktree 作って評価を実行
- `INDEX.md` のマージは自動で行う

## ログはファイルをメインにしたい

- 進行状況を stdout に流してると、凄い勢いで流れてしまうので状況がよくわからなくなる
- 途中経過のダンプとか、レポートとか、長くなりうるものは全部ファイルに
- stdout ニはそれらファイルのパスをだすだけ

# 補助的なサブコマンド

## `cmoc review session`

- `<cmoc-session-branch>` 上で行った変更を全て総合したら、結局どういうことなのか？　をレポートさせる
- おかしなことが起きていないかに気づくための判断材料

## `cmoc propose oracles`

- 仕様変更を自由記述して、それに対して必要な作業を提案してくれる
    - 「とりあえず書きました」みたいなのが多すぎる
    - 文章設計みたいな概念のない、メンテがしんどい感じの文章を平気で出してくる
    - だからソースコードのレイヤーでは AI 生成物と付き合わないことにしたという話なんだけど
    - 仕様のドラフトを AI に書かせるにしても、もうちょっとマシな品質のものを出してほしい
- 低品質な oracles を提案しないように
    - プロンプトをガッツリ組む
    - オーケストレーションをガッツリ組む
- 仕様書のコーディング規約が必要
    - 良い書き方、悪い書き方、そういうのを事細かに oracles に明示する必要がある

## `cmoc index` サブコマンド

- `INDEX.md` の構築だけを行うコマンド
- `INDEX.md` を最新に追従させてから Codex CLI を直接呼び出してなんかする…みたいな用途を想定

## `cmoc commit` サブコマンド

- 未コミット差分をコミットするだけ
- Codex CLI を使ってコミットメッセージを自動生成してくれる
- ただのヘルパー関数
- ついでなので cmoc が生成するコミットメッセージも規約化したほうが良いか

## `cmoc status` サブコマンド

- 現在のブランチが cmoc branch か
- base commit は何か
- 未コミット差分が oracle 内だけか
- 最後の review oracles / apply レポートはどれか
- apply が完了扱いか未完了扱いか
- merge 可能そうか

## `cmoc dector` サブコマンド

- codex が呼べるか
- Structured Output が使えるか
- .cmoc が gitignore されているか
- oracles/INDEX.md があるか
- 必要な git 状態か

## `cmoc kaizen fork` サブコマンド

- cmoc 自体を改善するための情報を収集してレポートする機能
- 本番運用上でのフィードバックループを回すための機能
- 必要な情報は `<repor-root>` 上でしか収集出来ないので、 cmoc のサブコマンドに組み込む事自体は正しい方向性なはず

## `cmoc version` サブコマンド

- ただのバージョン表示

# 未整理

## `<cmoc-branch>` をヒントとして渡す

- 「今の作業と関係のある変更は `<cmoc-branch>` に乗ってるからそれ見てね」を各コマンドでやりたい
- 主に「どこ見ていいか分からないから全チェック」を回避するのに使いたい
- 具体的にどういう注入のしかたするのかは gpt と相談
- 逆に、今までの経緯を注入することで、それに引っ張られてしまい新しい発見ができないかも
- やらなかった事リストへ

## そろそろ別プロダクトかも

- 開発環境みたいな様相を呈してきた
- もう minimal という感じではない

## oracles 記述言語が欲しい

- 多分、全てを自然言語で書くのではなく、構造化が必要
- 例えば、登場人物は「名前＋説明」みたいな辞書形式で書ける
- そうやって、無理のない範囲で構造化して、機械的処理の適性を改善したい
- 「LLM っていう自然言語を解釈可能な情報処理モジュール」の存在を前提とした新しい言語が必要
