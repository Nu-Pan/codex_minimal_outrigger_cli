# `bdist.linux-x86_64`

## Summary
- 内容を持たない空のビルド成果物用ディレクトリであり、現時点で同階層以下へルーティングすべき本文や下位要素はない。
- 再生成可能な成果物置き場に相当するため、正本仕様、実装、テスト、補助文書への入口としては機能しない。

## Read this when
- このディレクトリ自体が空であること、または読むべき下位ファイルが存在しないことを確認したいとき。
- ビルド成果物や一時生成物が管理対象に混入していないかを確認する作業で、この場所の現在内容を確認する必要があるとき。

## Do not read this when
- 正本仕様断片を確認したいとき。oracle 配下の本文を読む。
- 実装やテストの挙動を調べたいとき。src または test 配下の該当本文を読む。
- ビルド手順、配布設定、生成物の作成条件を調べたいとき。設定ファイルやビルド関連の実装本文を読む。

## hash
- e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855

# `lib`

## Summary
- ビルド成果物側の実装パッケージを集めた階層である。AI 呼び出しプロンプト構築、基礎モデル、互換 runtime 入口、共通 runtime helper、リポジトリ設定モデル、CLI エントリーポイント、利用者向けサブコマンド実行処理への入口を含む。
- 正本実装ではなく生成済みパッケージ側の配置として、CLI から呼ばれる上位 orchestration と、それを支える共通基盤・設定・基本型のどこへ進むべきかを切り分けるためのルーティング起点になる。

## Read this when
- ビルド成果物側で、CLI 公開コマンド、サブコマンド実行処理、共通 runtime helper、設定モデル、AI 呼び出しパラメータ構築、基礎データ型のどの領域を読むべきか判断したいとき。
- cmoc の実行時入口から、Typer アプリ、利用者向け workflow、Codex 呼び出し、設定、Git・ログ・状態・パスなどの共通処理までの接続関係をビルド成果物上で追いたいとき。
- 目次生成、TUI、session、apply、review oracle、init などの実装入口を探し、上位サブコマンド処理と共通 helper の責務境界を確認したいとき。
- モデル種別、reasoning effort、ファイルアクセスモード、Structured Output schema、path token、standard、構造化 Markdown 生成など、複数領域で共有される内部表現の入口を探したいとき。
- 旧 import 経路や公開 module 名が、責務別 runtime module や内部実装へどう接続されているかをビルド成果物側で確認したいとき。

## Do not read this when
- 正本の実装を変更したいとき。ここはビルド成果物側なので、対応するソース側の実装を読む方が適切である。
- oracle file、oracle standard、realization standard、INDEX.md 生成基準などの正本仕様断片そのものを確認したいとき。
- テストの期待値、fixture、検証観点を確認したいだけのときは、対応するテスト配置を直接読む。
- 個別の責務がすでに分かっており、AI 呼び出し構築、共通 runtime、設定、CLI 登録、サブコマンド実装などの下位対象へ直接進めるとき。
- 生成済み成果物ではなく、利用者が編集すべき実装元・仕様元・ドキュメント元を探しているとき。

## hash
- c0af99b1fb7e4822e822744bf450d8dd04d6ea973ffd8192416a65ff4b842121
