# `bdist.linux-x86_64`

## Summary
- 内容を持たない空のビルド成果物用ディレクトリであり、現時点で同階層以下へルーティングすべき本文や下位要素はない。
- 再生成可能な成果物置き場に相当するため、正本仕様、実装、テスト、補助文書への入口としては機能しない。

## Read this when
- このディレクトリ自体が空であること、または読むべき下位ファイルが存在しないことを確認したいとき。
- ビルド成果物や一時生成物が管理対象に混入していないかを確認する作業で、この場所の現在内容を確認する必要があるとき。

## Do not read this when
- 正本仕様断片を確認したいとき。oracle 配下の本文を読む。
- 実装やテストの挙動を調べたいとき。src または test 配下の該当本文を読む。
- ビルド手順、配布設定、生成物の作成条件を調べたいとき。設定ファイルやビルド関連の実装本文を読む。

## hash
- e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855

# `lib`

## Summary
- ビルド成果物側のライブラリ実装を集約する領域。AI 呼び出し用プロンプト構築、基礎モデル、互換 runtime 入口、共通 runtime helper、リポジトリ設定、CLI 登録、利用者向けサブコマンド実装へ進むための上位入口になる。
- 正本の実装領域ではなく、配布・ビルド後の実装配置として、CLI 公開面からサブコマンド orchestration、Codex 実行・設定・Git・ログ・状態管理などの共有基盤、プロンプト部品や内部データ型までの読む先を切り分ける階層である。

## Read this when
- ビルド成果物として配置された cmoc の実装で、CLI コマンド登録、サブコマンド実行制御、共通 runtime、設定モデル、AI 呼び出しパラメータ構築のどこへ進むべきかを切り分けたいとき。
- 利用者操作から内部処理までの入口を追い、トップレベル CLI がどの実装関数へ接続され、各操作が共通 helper や Codex 呼び出しへどうつながるか確認したいとき。
- Codex 実行、TUI 起動、preflight、profile、ログ、設定、Git、実行時パス、結果型、永続 state など、複数サブコマンドにまたがる実行時基盤の読む先を探したいとき。
- AI エージェント呼び出しに渡す role、goal、補助プロンプト、標準文書部品、ファイルアクセス制約、Structured Output schema などの組み立て経路を確認したいとき。
- cmoc 固有の基本型、パストークン、標準文書構造、Markdown 生成、設定 dataclass、モデル・推論努力・ファイルアクセスモードなどの内部表現をビルド成果物側から確認したいとき。

## Do not read this when
- 正本の実装を変更したいとき。ここはビルド成果物側なので、対応するソース側の実装を読む方が直接的である。
- oracle file、正本仕様断片、標準文書の人間管理上の内容、INDEX.md 生成規則そのものを確認したいときは、仕様側の文書や oracle 側の対象を読む。
- 個別のテスト期待値、fixture、検証観点を確認したいだけのときは、対応するテスト配置へ直接進む。
- 特定の CLI サブコマンドの詳細だけを調べたいときは、この階層全体ではなく、その操作単位の実装へ進む。
- 生成済みの INDEX.md、レビュー結果、TUI 実行結果、レポートなど、AI 呼び出し後の成果物の内容を確認したいだけのとき。

## hash
- 7cd660c67056c64a18d64463d24df4356ae42e429fcc46ea7a3e902a13581847
