{
  "type": "object",
  "additionalProperties": false,
  "required": [
    "findings"
  ],
  "properties": {
    "findings": {
      "type": "array",
      "description": "レビュー対象 oracle file と関連 oracle file から見つけた、既知の関連所見と重複しない新規所見のリスト。新規所見が無い場合は空配列を返す。",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "severity",
          "title",
          "oracle_path",
          "reason"
        ],
        "properties": {
          "severity": {
            "type": "string",
            "description": "所見の重大度。仕様断片同士の明確な矛盾、または仕様に従う実装時に実装者裁量では解消不能な問題は fatal。日本語的な誤り、誤字・脱字・助詞抜け、用語不統一・表記揺れ・typo など表記上の単純な問題は minor。",
            "enum": [
              "fatal",
              "minor"
            ]
          },
          "title": {
            "type": "string",
            "description": "所見の短い見出し。問題の要点が分かるように簡潔に書く。"
          },
          "oracle_path": {
            "type": "string",
            "description": "所見の主な根拠となる oracle file のパス。レビュー対象または関連する oracle file を指す。"
          },
          "reason": {
            "type": "string",
            "description": "なぜ新規所見として扱うべきかの理由。oracle file の記述に基づき、既知の関連所見との差分も分かるように書く。"
          }
        }
      }
    }
  }
}
