# `oracles`

## Summary

- `cmoc review oracles` で使うレビュー用 Structured Output schema をまとめた目次です。
- 所見の列挙、統合、妥当性検証、採否判定の各 JSON schema への入口です。
- `enumerate_findings.json`、`merge_findings.json`、`validate_findings_advocate.json`、`validate_findings_challenger.json`、`judge_findings.json` を案内します。

## Read this when

- `cmoc review oracles` の schema の役割や使い分けを確認したいとき。
- 所見の列挙・マージ・検証・判定のどの schema を読むべきか迷ったとき。
- review 用 Structured Output schema を追加・修正・レビューしているとき。

## Do not read this when

- `cmoc review oracles` の実行手順やレビューの流れそのものだけを確認したいとき。
- review 以外の Structured Output schema を探しているとき。
- `oracles` 全体や上位階層のルーティング方針だけを確認したいとき。

## hash

- c0b2749c12af8ff5cf4a9185c4f518860937a255cddc2dd0ebcd7b902c712833
