"""oracle.acp_builder.indexing を acp.builder.indexing として公開する互換入口。

`<work-root>/oracle/src/oracle/acp_builder/indexing` を正本に保ったまま既存の
`acp.builder.indexing.*` 参照を成立させるために残す。削除条件は
realization 側と利用者向け公開面から同参照がなくなること。
"""
