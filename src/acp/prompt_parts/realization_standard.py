"""realization standard 全体の prompt 本文を単一責務として構築する。

対応する oracle file は
`<work-root>/oracle/src/acp/prompt_parts/realization_standard.py`。
このファイルは 16,000 文字を超えるが、責務境界は realization file の品質基準を
StructDoc 化する prompt part に閉じている。各 Standard は、サイズ、コメント、
責務分割、抽象化、公開面、テスト、依存関係、完了時点検をまたいで
相互参照される 1 つの規範集合であり、分割すると生成順序と文脈の確認に
複数 module を読む必要が生じる。
現状は同じ prompt 本文として同時に読む方が凝集性と読み取り文脈を保てる。
"""

# cmoc
from basic.struct_doc import StructDoc
from basic.standard import (
    Requirement as Req,
    Standard,
    standard_to_struct_doc,
)


def build_realization_standard() -> StructDoc:
    standards = [
        Standard(
            title="realization file の総文字数の最小化を目標とする",
            backgrounds=[
                "realization file の規模が大きいほど、実装担当者である AI に与える課題の難易度が上がる",
                "realization file の規模が大きいほど、トークン消費が増える",
            ],
            requirements=[
                Req(
                    "推奨",
                    "必ず守らなければならない要件を満たしている範囲内（解空間内）で、"
                    "realization file 全体で見た時の総文字数が最小となること"
                    "（文字数最小解）を目指す",
                ),
                Req(
                    "推奨",
                    "複数箇所で出現するよく似た処理は関数化し、それを各所で使い回す",
                ),
                Req(
                    "推奨",
                    "意味的に重複している実装は 1 つに集約する",
                ),
                Req(
                    "必須",
                    "現行の仕様と関係のない、過去の仕様に基づく実装は削除する",
                ),
                Req(
                    "禁止",
                    "同じ意味・同じ責務の実装を複数箇所に重複させてはいけない",
                ),
                Req(
                    "禁止",
                    "現行仕様では使われない旧仕様向け実装・テスト・コメントを残してはいけない",
                ),
                Req(
                    "禁止",
                    "文字数削減のために可読性や意味の明確さを損なってはいけない",
                ),
            ],
            examples=[
                "3 箇所で同じ path 正規化をしており、入力と失敗時挙動も同じなら 1 つの helper に集約する",
                "仕様変更後に到達不能になった旧分岐は、対応する旧テストも含めて削除する",
                "短縮名にすると呼び出し側の意味が読めなくなる場合は、文字数より明確な命名を優先する",
            ],
        ),
        Standard(
            title="realization file の高品質化は望ましいことである",
            backgrounds=[
                "realization file は cmoc により継続的にメンテナンスされる",
                "メンテナンスにかかるコストは、AI が読む文脈量や判断すべき分岐量の影響を受ける",
                "realization file の品質には、変更容易性・責務の明確さ・不要部分の少なさが含まれる",
            ],
            requirements=[
                Req(
                    "推奨",
                    "realization file は、現行仕様を満たすために必要な情報だけを持つ",
                ),
                Req(
                    "推奨",
                    "realization code は、責務・入力・出力・副作用が読み取りやすい単位に分割する",
                ),
                Req(
                    "禁止",
                    "読み取りやすさを損なう過度な圧縮をしてはいけない",
                ),
                Req(
                    "禁止",
                    "将来使うかもしれないだけの抽象化を追加してはいけない",
                ),
                Req(
                    "必須",
                    "実装だけから読み取りにくい意図・制約・根拠は、コメントまたは docstring に書く",
                ),
                Req(
                    "禁止",
                    "シグネチャ・型名・実装から明らかな情報だけを、コメントや docstring で冗長に繰り返してはいけない",
                ),
                Req(
                    "必須",
                    "現行仕様・現行実装から不要になった realization file、関数、クラス、分岐、設定、テスト、コメントは削除する",
                ),
                Req(
                    "必須",
                    "各ファイル・関数・クラスの責務が読み取れるようにする",
                ),
                Req(
                    "必須",
                    "実装の意図や制約は、必要な範囲でコメントまたは docstring に残す",
                ),
                Req(
                    "禁止",
                    "現行仕様に関係しない古い実装・コメント・補助ファイルを残してはいけない",
                ),
                Req(
                    "禁止",
                    "抽象化や分割により、読むべき文脈量を不要に増やしてはいけない",
                ),
            ],
            examples=[
                "外部コマンドの戻り値をあえて握りつぶす場合は、その制約がコードから読みにくいので短くコメントする",
                "現行仕様が 1 形式だけを扱うなら、将来の形式追加を理由に汎用 parser 層を先に作らない",
                "型注釈と関数名で入力・戻り値が分かる関数では、docstring に同じ情報だけを繰り返さない",
            ],
        ),
        Standard(
            title="コメントや docstring は実装意図と根拠を補う",
            backgrounds=[
                "realization code は AI が継続的に変更するため、局所的なコードだけでは判断しにくい意図や制約が失われやすい",
                "一方で、コードの近くから分かる内容をコメントで繰り返すと、読むべき文脈と更新漏れの危険が増える",
            ],
            requirements=[
                Req(
                    "必須",
                    "コードだけから読み取りにくい実装意図は、コメントに書く",
                ),
                Req(
                    "必須",
                    "その実装方針でなければならない根拠がある場合は、コメントに書く",
                ),
                Req(
                    "必須",
                    "あえて取らなかった実装方針が将来も選ばれやすい場合は、その方針を取らなかった理由を書く",
                ),
                Req(
                    "必須",
                    "対応する oracle file が存在する場合は、コメントに `<work-root>` トークン起点の oracle file path を書く",
                ),
                Req(
                    "必須",
                    "コメントや docstring は、その前後のコードを読むだけでは分からない情報を書く",
                ),
                Req(
                    "禁止",
                    "コメントや docstring で、その前後のコードを読むだけで分かることを説明してはいけない",
                ),
                Req(
                    "推奨",
                    "判断が難しい分岐、外部制約、互換性維持、性能・安全性・失敗時挙動の選択理由は、短いコメントで根拠を残す",
                ),
                Req(
                    "禁止",
                    "処理手順の逐語的な説明、型名・関数名・変数名の言い換え、古い仕様の説明だけのコメントを残してはいけない",
                ),
            ],
            examples=[
                "oracle file の明示要求に対応する処理には、必要に応じて "
                "`<work-root>/oracle/doc/...` のように根拠 path を近くに残す",
                "単純な代入や関数呼び出しには `値を代入する` のようなコメントを書かない",
                "一見自然なライブラリ利用を避けている場合は、依存増加や失敗時挙動の違いなど、避けた理由を短く書く",
            ],
        ),
        Standard(
            title="realization file は意味上のまとまりと適度なサイズに保つ",
            backgrounds=[
                "cmoc は INDEX.md を手がかりに必要な file だけを読みに行くことで、AI のトークン消費を節約する",
                "このルーティングが有効に働くためには、各 realization file のサイズ感がある程度揃っている必要がある",
                "巨大な realization file は、INDEX.md で読む先を絞れても局所的に読むべき文脈を増やす",
            ],
            requirements=[
                Req(
                    "必須",
                    "file の分割・統合は、コードの意味、責務境界、同時に読む必要がある文脈を主な根拠に判断する",
                ),
                Req(
                    "必須",
                    "1 つの realization file が複数の独立した責務を持つ場合は、責務境界に沿って分割する",
                ),
                Req(
                    "推奨",
                    "1 つの realization file が 8,000 文字を超える場合は、意味上自然な分割点がないか確認する",
                ),
                Req(
                    "必須",
                    "1 つの realization file が 16,000 文字を超える場合は、"
                    "分割しない理由を責務境界・凝集性・読み取り文脈の観点で"
                    "説明できなければならない",
                ),
                Req(
                    "必須",
                    "小さすぎる realization file が同じ責務や同じ変更理由で常に一緒に読まれる場合は、統合を検討する",
                ),
                Req(
                    "禁止",
                    "文字数だけを根拠に、強く結合した処理を不自然に分割してはいけない",
                ),
                Req(
                    "禁止",
                    "INDEX.md で読む先を選べても、選んだ先が巨大 file になり、局所的な読解ができない状態を放置してはいけない",
                ),
            ],
            examples=[
                "CLI command 定義、入出力変換、永続状態操作が同じ file に混在して 16,000 文字を超えるなら、責務ごとの分割を検討する",
                "互いに短い helper file 群が同じ変更で常に同時に読まれるなら、1 つの責務を持つ module へ統合する",
                "8,000 文字を超えていても、単一の schema 定義として意味上ひとまとまりなら、分割より命名や局所的な構造化を優先してよい",
            ],
        ),
        Standard(
            title="realization code の追加は既存 realization code の整理と一体で行う",
            backgrounds=[
                "コードベースの肥大化は、新しいファイル・関数・クラスを追加した時だけでなく、古い実装を残した時にも発生する",
                "既存実装の近くに同じ責務の実装が残ると、AI はどちらが正しいかを判断するために余計な文脈を読む必要がある",
            ],
            requirements=[
                Req(
                    "必須",
                    "realization code を追加する前に、同じ責務または近い責務を持つ既存の realization code を確認する",
                ),
                Req(
                    "禁止",
                    "既存実装の修正・移動・置換で現行仕様を満たせる場合は、新しい実装を追加してはいけない",
                ),
                Req(
                    "必須",
                    "新しい実装を追加した場合は、置き換えられた古い実装、不要になった分岐、重複した定数、古いテストも同時に削除または統合する",
                ),
                Req(
                    "禁止",
                    "一時的な移行コード・互換コードは、現行仕様から明示的に必要な場合以外は残してはいけない",
                ),
                Req(
                    "必須",
                    "移行コード・互換コードを残す場合は、それを削除できる条件を近くのコメントに書く",
                ),
                Req(
                    "禁止",
                    "新旧の実装経路を同じ責務を持ったまま並存させてはいけない",
                ),
                Req(
                    "禁止",
                    "新しい実装に置き換えられた古いテスト・fixture・定数を残してはいけない",
                ),
                Req(
                    "必須",
                    "移行コードや互換コードには、残す理由と削除条件を書く",
                ),
            ],
            examples=[
                "新 parser へ置き換える場合は、旧 parser の呼び出し経路、専用 fixture、専用テストも同じ変更で整理する",
                "既存関数に小さな引数追加で現行仕様を満たせるなら、別名のほぼ同じ関数を追加しない",
                "`legacy_` の分岐を残す必要があるなら、削除条件を近くに書けない時点で残す根拠が弱いと判断する",
            ],
        ),
        Standard(
            title="新しい抽象化は実在する重複または明確な責務境界に基づく",
            backgrounds=[
                "抽象化はコード量を減らすこともある",
                "抽象化は呼び出し関係・引数・概念を増やしてコードベースを肥大化させることもある",
                "「将来使うかもしれない」抽象化は、現時点の AI の読解対象を増やす",
            ],
            requirements=[
                Req(
                    "必須",
                    "新しい関数・クラス・モジュールは、現行仕様上の責務境界を明確にする場合、または実在する重複を削減する場合に作る",
                ),
                Req(
                    "禁止",
                    "単に処理が短いという理由だけで、1 箇所からしか呼ばれない小関数を増やしてはいけない",
                ),
                Req(
                    "必須",
                    "共通化は、見た目が似ているだけではなく、入力・出力・失敗時挙動・不変条件が同じ場合に行う",
                ),
                Req(
                    "禁止",
                    "呼び出し元ごとの差分を大量のフラグや callback で吸収する汎用関数を作ってはいけない",
                ),
                Req(
                    "推奨",
                    "共有範囲が 1 モジュール内に閉じる場合は、そのモジュール内の private helper に留める",
                ),
                Req(
                    "必須",
                    "共有モジュールへ移動するのは、複数のサブコマンドまたは複数の上位モジュールから使うことが明確な場合に限る",
                ),
                Req(
                    "必須",
                    "新しい抽象化は、実在する重複または明確な責務境界に対応させる",
                ),
                Req(
                    "禁止",
                    "抽象化により、呼び出し側の理解に必要な引数・フラグ・callback を過度に増やしてはいけない",
                ),
                Req(
                    "必須",
                    "共通化された処理の入力・出力・失敗時挙動・不変条件は揃える",
                ),
                Req(
                    "必須",
                    "共有モジュールに置く処理は、実際に複数箇所から使われる見込みを持っていなければならない",
                ),
            ],
            examples=[
                "同じ入力・出力・失敗時挙動を持つ path 変換が複数箇所にあるなら、共通 helper にまとめる",
                "git command を呼ぶ点だけが似ていて失敗時の扱いが違う処理は、1 関数にまとめない",
                "1 箇所からしか呼ばれない 3 行の処理は、名前を付けることで責務境界が明確になる場合だけ関数化する",
            ],
        ),
        Standard(
            title="公開面・設定面・状態の増加を抑制する",
            backgrounds=[
                "CLI 引数、サブコマンド、設定項目、環境変数、出力 schema、保存ファイルは、一度追加すると互換性維持の対象になりやすい",
                "公開面が増えると、実装・テスト・ドキュメント・利用者理解のすべてが増える",
            ],
            requirements=[
                Req(
                    "必須",
                    "新しい CLI 引数、サブコマンド、設定項目、環境変数、出力項目、状態ファイルは、現行仕様を満たすために必要な場合だけ追加する",
                ),
                Req(
                    "禁止",
                    "内部実装の都合だけで公開面を増やしてはいけない",
                ),
                Req(
                    "推奨",
                    "単一の妥当な挙動を選べる場合は、設定項目で利用者に選ばせない",
                ),
                Req(
                    "必須",
                    "新しい永続状態を追加する場合は、その生成条件・更新条件・削除条件を仕様または実装上で明確にする",
                ),
                Req(
                    "禁止",
                    "同じ情報をログ・状態ファイル・出力 schema に重複して保存してはいけない",
                ),
                Req(
                    "必須",
                    "追加する公開面は、現行仕様上の必要性に対応していなければならない",
                ),
                Req(
                    "禁止",
                    "内部実装の都合だけで利用者向けの選択肢を増やしてはいけない",
                ),
                Req(
                    "必須",
                    "永続状態のライフサイクルが読み取れるようにする",
                ),
                Req(
                    "禁止",
                    "同じ情報を複数の保存先・出力先に重複させてはいけない",
                ),
            ],
            examples=[
                "利用者が選ぶ必要のない内部 retry 回数は、CLI option ではなく実装内の単一方針として扱う",
                "状態ファイルを追加する場合は、どの操作で作られ、どの成功/失敗で更新・削除されるかを実装から追えるようにする",
                "計算で再現でき、利用者にも外部連携にも見せない値は、ログと状態ファイルの両方に保存しない",
            ],
        ),
        Standard(
            title="realization test の肥大化も抑制する",
            backgrounds=[
                "realization test も realization code の一部であり、読み取り・保守・更新の対象である",
                "テストが重複すると、仕様変更時に実装よりテスト修正のほうが大きくなる",
            ],
            requirements=[
                Req(
                    "必須",
                    "テストは、実装詳細ではなく、現行仕様上意味のある外部挙動または制御ロジックを検証する",
                ),
                Req(
                    "推奨",
                    "同じ観点のテストケースは、可能な範囲で parametrized test や小さな fixture に集約する",
                ),
                Req(
                    "必須",
                    "似たテストを追加する前に、既存テストへケース追加できないか確認する",
                ),
                Req(
                    "必須",
                    "古い仕様に対応するテスト、削除済み実装に対応するテスト、同じ失敗を重複して検出するテストは削除または統合する",
                ),
                Req(
                    "推奨",
                    "大きな fixture ファイルは、現行仕様の検証に必要な最小内容にする",
                ),
                Req(
                    "禁止",
                    "Codex CLI や LLM の出力品質そのものを検証するためのテストを追加してはいけない",
                ),
                Req(
                    "必須",
                    "テストは現行仕様上意味のある挙動または制御ロジックを検証する",
                ),
                Req(
                    "禁止",
                    "同じ観点のテストを複数のテスト関数に不要に分散させてはいけない",
                ),
                Req(
                    "禁止",
                    "削除済み実装や旧仕様に対応するテストを残してはいけない",
                ),
                Req(
                    "禁止",
                    "fixture を検証対象に対して過大にしてはいけない",
                ),
            ],
            examples=[
                "入力値違いで同じ外部挙動を確認する正常系は、個別関数を増やさず parametrized test にまとめる",
                "内部 helper の呼び出し順は変わり得るので、仕様上の出力や副作用で同じ制御を検証する",
                "小さな文字列入力で検証できる parser テストには、実プロジェクト規模の fixture を追加しない",
            ],
        ),
        Standard(
            title="依存関係・補助ファイル・生成物の増加を抑制する",
            backgrounds=[
                "外部依存、補助スクリプト、テンプレート、生成済みファイルは、実装本体以外にもメンテナンス対象を増やす",
                "realization ancillary の増加は、AI が読むべき文脈と更新すべき対象を増やす",
            ],
            requirements=[
                Req(
                    "必須",
                    "新しい外部依存は、標準ライブラリまたは既存依存で現行仕様を十分に満たせない場合だけ追加する",
                ),
                Req(
                    "推奨",
                    "外部依存を追加する場合は、それにより自前実装・テスト・保守負担がどれだけ減るかを重視する",
                ),
                Req(
                    "禁止",
                    "小さな処理のためだけに大きな依存を追加してはいけない",
                ),
                Req(
                    "禁止",
                    "生成可能なキャッシュ、実行ログ、一時ファイル、ビルド成果物を realization file として持ってはいけない",
                ),
                Req(
                    "必須",
                    "補助スクリプトを追加する場合は、既存の CLI・テスト・開発手順で代替できないか確認する",
                ),
                Req(
                    "必須",
                    "追加する外部依存には、標準ライブラリまたは既存依存では代替しにくい理由がなければならない",
                ),
                Req(
                    "必須",
                    "外部依存を追加する場合は、総合的な実装・テスト・保守負担が減っていなければならない",
                ),
                Req(
                    "禁止",
                    "再生成可能なファイルを正本のように管理してはいけない",
                ),
                Req(
                    "禁止",
                    "補助スクリプトを既存の CLI・テスト・開発手順と重複させてはいけない",
                ),
            ],
            examples=[
                "標準ライブラリで安全に扱いにくい構文解析なら、小さく一般的な依存で自前 parser とテストを減らせるかを見る",
                "数行の path 操作は既存依存または標準ライブラリで足りるため、外部パッケージ追加の根拠にしない",
                "既存のテストコマンドで再生成できる成果物は、確認用に作っても realization file として管理しない",
            ],
        ),
        Standard(
            title="realization file の変更完了時には削除・統合余地を確認する",
            backgrounds=[
                "肥大化は、実装中よりも実装完了時に見落とされやすい",
                "「追加して動いた」状態は、「最小で保守しやすい」状態とは限らない",
            ],
            requirements=[
                Req(
                    "必須",
                    "realization file の変更完了前に、追加したもののうち削除・統合・短縮できるものがないか確認する",
                ),
                Req(
                    "禁止",
                    "未使用の関数、クラス、import、定数、fixture、補助ファイルを残してはいけない",
                ),
                Req(
                    "禁止",
                    "同じ責務を持つ名前違いの実装を残してはいけない",
                ),
                Req(
                    "禁止",
                    "現行仕様の説明に不要な TODO、NOTE、コメントを残してはいけない",
                ),
                Req(
                    "推奨",
                    "変更後の realization file は、現行仕様を満たすための最小構成にする",
                ),
                Req(
                    "禁止",
                    "追加した実装・テスト・補助ファイルに、削除・統合・短縮できるものを残してはいけない",
                ),
                Req(
                    "禁止",
                    "未使用の import、定数、fixture、helper を残してはいけない",
                ),
                Req(
                    "禁止",
                    "同じ責務を持つ実装を名前違いで並存させてはいけない",
                ),
                Req(
                    "必須",
                    "TODO、NOTE、コメントは現行仕様の理解に寄与していなければならない",
                ),
            ],
            examples=[
                "実装中に作った確認用 helper は、最終コードから呼ばれないなら完了前に削除する",
                "新しい共通関数へ移行した後は、旧 helper 本体だけでなく import と専用テストも削除対象として確認する",
                "仕様理解に寄与しない `念のため残す` だけの TODO は、現行仕様の説明ではないので残さない",
            ],
        ),
    ]
    return StructDoc(
        "realization standard",
        *[standard_to_struct_doc(rs) for rs in standards],
    )
