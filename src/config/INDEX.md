# `__init__.py`

## Summary
- 設定定義の正本を oracle 側に保ったまま、既存の `config.*` 参照を受け入れるパッケージ入口としての役割を記す。
- realization と利用者向け公開面の双方から `config.*` 参照がなくなれば、この互換入口は削除できる。

## Read this when
- 既存の `config.*` 参照を維持するために、パッケージ入口が必要かを判断するとき。
- パッケージ単位の互換性を調べるとき。具体的な設定型や再公開内容は、そのサブモジュールと oracle 側の定義を確認する。

## Do not read this when
- 設定型のフィールド、既定値、または再公開されるシンボルを調べるときは、パッケージ入口ではなく該当するサブモジュールと oracle 側の定義を読む。
- 永続化された設定の読み書きや実行時の挙動を調べるときは、その実装を直接確認する。

## hash
- 2df099916bfadae1547b9bb803be4606e032bc2d738c77e453dddf5756e2dece

# `cmoc_config.py`

## Summary
- 正本の設定定義を再公開し、既存の設定モジュール別 import との互換性を保つ入口です。

## Read this when
- 既存の設定モジュール別 import の互換性を調べる、またはその参照がなくなったかを確認して、この入口の削除可否を判断するとき。

## Do not read this when
- 設定項目や既定値そのものを変更するときは、正本の oracle 側設定定義を確認してください。
- パッケージ全体の設定 import 入口を調べるときは、隣接するパッケージ初期化ファイルを確認してください。

## hash
- e143f75a879b3dc46fb34ebbab3e7e3a0cf88bb6d8e061e1f98dfb2859e1aa87
