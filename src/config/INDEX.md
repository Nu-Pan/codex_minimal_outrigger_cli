# `__init__.py`

## Summary
- oracle src 側の config パッケージと互換の import 入口を提供する、実装側 config パッケージの初期化ファイル。現時点では入口であることだけを示し、設定処理本体や具体的な設定値はここには持たない。

## Read this when
- config パッケージを import したときの入口としての位置づけを確認したいとき。
- oracle src 側の config import との互換入口が実装側に存在するかを確認したいとき。

## Do not read this when
- 設定値の定義、読み込み、検証、変換などの具体的な挙動を調べたいとき。
- config 配下の個別モジュールや設定処理本体の責務を確認したいとき。

## hash
- 639ce7cef3a11b770f9c0a4012cdcf8143deef886a50cbfa6baa08048cadf76f

# `cmoc_config.py`

## Summary
- 正本側に置かれた設定定義を、実装側の設定モジュールとしてそのまま再公開する薄い入口。独自の設定値、変換処理、検証処理は持たず、設定実体への互換的な参照点として位置づく。

## Read this when
- 実装側から設定定義を参照する import 経路を確認したいとき。
- 設定定義の実体が実装側ではなく正本側に委譲されているかを確認したいとき。
- 設定モジュールの公開名や再エクスポートの境界を確認したいとき。

## Do not read this when
- 個々の設定項目の意味、既定値、制約を知りたいとき。この対象は定義本体ではなく再公開入口だけを持つ。
- 設定値の読み込み、解決、検証、永続化などの処理を調べたいとき。この対象には処理ロジックはない。
- 正本仕様として設定内容を確認したいとき。実体を持つ正本側の定義を直接読む方が適切である。

## hash
- 0675289475ac1012176020fde604409f7701107756d2a75c65729feb0963db13
