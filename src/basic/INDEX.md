# `acp.py`

## Summary
- AI コーディングエージェント呼び出しに渡す論理パラメータを定義する実装。モデルクラス、Reasoning effort、ファイルアクセスモードをバックエンド非依存の列挙として表し、プロンプト本文と Structured Output schema の有無を含む呼び出し単位のデータ構造をまとめる。
- バックエンドが受理する具体的なモデル名や権限表現ではなく、cmoc 内部で扱う抽象的な呼び出し条件の入口として位置づけられる。

## Read this when
- エージェント呼び出しに必要なパラメータ一式の型や保持項目を確認・変更したいとき。
- モデル品質・コストの抽象区分、Reasoning effort、ファイルアクセスモードの選択肢を確認・変更したいとき。
- Structured Output schema を要求する呼び出しと要求しない呼び出しを、呼び出しパラメータ上でどう表すか確認したいとき。
- プロンプト本文と実行条件をひとまとまりの値として受け渡す箇所を追うとき。

## Do not read this when
- 論理的なモデルクラスやファイルアクセスモードを、特定バックエンドが受理する具体的な値へ解決する処理を探しているとき。
- エージェントプロセスの起動、外部コマンド実行、API 呼び出し、入出力処理そのものを調べたいとき。
- 個別タスクのプロンプト生成内容や Structured Output schema の中身を調べたいとき。
- oracle file と realization file の一般定義や編集責務そのものを確認したいだけのとき。

## hash
- 7032992e51209db0ad3f986567a32da4745971b97c20870ac4bd25fb62492351

# `path_model.py`

## Summary
- cmoc で使うルートトークン表記と実パスの相互変換を担う実装。`<cmoc-root>`、`<repo-root>`、`<run-root>`、`<work-root>` の定義に対応し、絶対パス、ルートトークン付き相対パス、git worktree の探索規則を扱う。
- ルートトークン一覧、トークン付きパスの解決、実パスからトークン表記への変換、各 root の探索方法を確認するための入口となる。

## Read this when
- cmoc 内でパス表記を実際の絶対パスへ解決する処理を変更・確認したいとき。
- `<cmoc-root>`、`<repo-root>`、`<run-root>`、`<work-root>` の意味や検出条件を実装側で確認したいとき。
- ルートトークンを含まない相対パスの拒否、空パスの扱い、実パスが指定トークン配下にない場合の失敗挙動を確認したいとき。
- git の main worktree、linked worktree、現在の作業 worktree の判定に関わる不具合や仕様差を調べるとき。

## Do not read this when
- CLI コマンドの引数定義、表示文言、JSON 出力 schema など、利用者向けインターフェースを調べたいだけのとき。
- ファイルの読み書き、コピー、生成、削除など、パス解決後の入出力処理そのものを調べたいとき。
- oracle file と realization file の概念定義や管理方針を確認したいとき。
- テストケースの期待値や検証観点だけを確認したいとき。

## hash
- 2682a93c4fc3041b1037a749e0478cef28f038f7a7731a591b269af4ff5f00fd

# `standard.py`

## Summary
- 規範文書を表す実装モデルを定義し、見出し、背景、要求、判断例を検証して保持する。
- 規範の要求項目をラベル付き本文として表すデータ構造と、規範文書モデルを構造化ドキュメントへ変換する処理を扱う。
- oracle 側の規範文書モデルを realization として具体化する入口であり、規範から表示用・文書用の構造化表現を作る箇所を確認するための対象である。

## Read this when
- 規範文書をコード上でどう表現するか、または必須フィールドの検証条件を確認したいとき。
- 規範の背景、要求、判断例がどのように保持され、外部から参照されるかを確認したいとき。
- 要求項目のラベル種別や本文の扱いを確認したいとき。
- 規範文書モデルから構造化ドキュメントへの変換形式を確認または変更したいとき。
- oracle 側の規範文書定義に対応する realization 実装を確認したいとき。

## Do not read this when
- 自然言語で書かれた個別の規範本文そのものを確認したいだけのとき。
- 構造化ドキュメント一般のデータ構造や描画・出力処理を確認したいとき。
- 規範文書以外のモデル、CLI 操作、パス概念、実行状態、テストの責務を調べたいとき。
- INDEX.md のルーティング規則やエントリー生成規則そのものを確認したいとき。

## hash
- 30bfda8ed16cb74d8baa3e90415c45a627f0571f5f4cbb7cd77b5781d0ed0347

# `struct_doc.py`

## Summary
- 階層化された自然言語文書を Markdown に変換するための小さな実装で、見出し階層、本文文字列、コードフェンス付きコードブロックを表すクラスとレンダリング関数を提供する。
- 本文やコードブロックに含める triple quoted string 由来の余分な先頭末尾空行と共通インデントを整え、レンダリング結果では連続空行を 1 行に畳む。

## Read this when
- 構造化された見出しツリーから Markdown 文字列を生成する処理を確認・変更したいとき。
- 文書本文やコードブロックのインデント正規化、空行整理、Markdown 見出し深さの扱いを確認したいとき。
- 自然言語の階層文書を組み立てるためのデータ構造や、受け付ける子要素の型制約を確認したいとき。

## Do not read this when
- CLI コマンド、ファイル探索、永続状態、外部入出力など、Markdown レンダリング以外の挙動を調べたいとき。
- 生成された Markdown の利用先や保存先を調べたいだけで、文書構造から文字列へ変換する内部処理には関心がないとき。
- INDEX.md のルーティング規則や正本仕様断片そのものを確認したいとき。

## hash
- 09d1b79c73e8aa03329829672e0eaa69b9d2e7cd26d4e740f114ea2ef8c01ab4
