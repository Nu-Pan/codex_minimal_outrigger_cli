{
  "type": "object",
  "description": "realization refactor のファイル単位レビュー・修正 agent call の出力。",
  "additionalProperties": false,
  "required": [
    "findings"
  ],
  "properties": {
    "findings": {
      "type": "array",
      "description": "この agent call が返す所見と対応結果のリスト。所見がない場合は空配列。",
      "items": {
        "type": "object",
        "description": "この agent call で発見した所見 1 件と、その対応結果。",
        "additionalProperties": false,
        "required": [
          "title",
          "evidences",
          "changed_paths",
          "oracle_requirement",
          "observed_implementation",
          "reason",
          "resolution"
        ],
        "properties": {
          "title": {
            "type": "string",
            "description": "所見の短い見出し。"
          },
          "evidences": {
            "type": "array",
            "description": "調査時点の所見の根拠となる文言の位置情報。",
            "minItems": 1,
            "items": {
              "type": "object",
              "description": "調査時点の所見を裏付ける位置情報 1 件。",
              "additionalProperties": false,
              "required": [
                "path",
                "line_start",
                "line_end",
                "summary"
              ],
              "properties": {
                "path": {
                  "type": "string",
                  "description": "所見の根拠となる file の絶対 path。"
                },
                "line_start": {
                  "type": [
                    "integer",
                    "null"
                  ],
                  "description": "所見の根拠となる記述の開始行。行番号を特定できない場合は null。"
                },
                "line_end": {
                  "type": [
                    "integer",
                    "null"
                  ],
                  "description": "所見の根拠となる記述の終了行。行番号を特定できない場合は null。"
                },
                "summary": {
                  "type": "string",
                  "description": "該当箇所の短い要約。修正による位置情報の変化を判別するための情報。"
                }
              }
            }
          },
          "changed_paths": {
            "type": "array",
            "description": "この所見への対応による realization file の net 差分を申告する path の配列。対応する net 差分がない場合は空配列。",
            "items": {
              "type": "string",
              "description": "agent call 開始時点を基準として終了時点に残る net 差分の、正規化済み `{{work-root}}` 相対 path。追加と変更は終了時点の path、削除は開始時点の path、rename は rename 前後の path で表す。"
            }
          },
          "oracle_requirement": {
            "type": "string",
            "description": "所見に関係する oracle file の要求、または対応する明示要求がない旨。"
          },
          "observed_implementation": {
            "type": "string",
            "description": "調査時点の実装が実際にどうなっていたか。"
          },
          "reason": {
            "type": "string",
            "description": "所見を要修正とする理由。"
          },
          "resolution": {
            "type": "object",
            "description": "所見への対応結果。",
            "additionalProperties": false,
            "required": [
              "status",
              "summary",
              "verification"
            ],
            "properties": {
              "status": {
                "type": "string",
                "enum": [
                  "fixed",
                  "unresolved"
                ],
                "description": "所見の対応状態。fixed は修正済みとして報告し、unresolved は未解消として報告する。"
              },
              "summary": {
                "type": "string",
                "description": "実施した修正、または未解消となった理由の要約。"
              },
              "verification": {
                "type": "string",
                "description": "修正後に実施した検証と、その結果の要約。"
              }
            }
          }
        }
      }
    }
  }
}
